---
// Astro component for property filters UI only
---

<div class="w-full max-w-5xl mx-auto mt-10 p-4 bg-white rounded-md lg:rounded-[4.5rem] shadow-2xl flex flex-col md:flex-row gap-6 i border border-gray-100">
  <div class="flex-1 w-full flex flex-col justify-start items-start">
    <label class="block text-xs font-semibold text-gray-400 pl-1.5 pb-1 " for="category">Categoría</label>
    <select id="category" name="category" class="w-full rounded-2xl border-2 border-gray-200 bg-gray-50 px-4 py-2.5 text-gray-700 focus:outline-none focus:ring-2 focus:ring-pink-500 focus:border-pink-500 transition shadow-sm">
      <option value="">Todas las categorías</option>
      <option value="apartamento">Apartamento</option>
      <option value="casa">Casa</option>
      <option value="finca">Finca</option>
      <option value="habitacion">Habitación</option>
      <!-- Agrega más categorías si es necesario -->
    </select>
  </div>
  <div class="flex-1 w-full flex flex-col justify-end">
    <label class="block text-xs font-semibold text-gray-700 mb-2 ml-1" for="type">Tipo</label>
    <select id="type" name="type" class="w-full rounded-2xl border-2 border-gray-200 bg-gray-50 px-4 py-2.5 text-gray-700 focus:outline-none focus:ring-2 focus:ring-pink-500 focus:border-pink-500 transition shadow-sm">
      <option value="">Todos los tipos</option>
      <option value="alquiler">Alquiler</option>
      <option value="venta">Venta</option>
    </select>
  </div>
  <div class="flex-1 w-full flex flex-col justify-end">
    <label class="block text-xs font-semibold text-gray-700 mb-2 ml-1" for="city">Ciudad</label>
    <select id="city" name="city" class="w-full rounded-2xl border-2 border-gray-200 bg-gray-50 px-4 py-2.5 text-gray-700 focus:outline-none focus:ring-2 focus:ring-pink-500 focus:border-pink-500 transition shadow-sm">
      <option value="">Todas las ciudades</option>
      <option value="medellin">Medellín</option>
      <option value="bogota">Bogotá</option>
      <option value="cali">Cali</option>
      <option value="cartagena">Cartagena</option>
      <option value="santa-marta">Santa Marta</option>
      <option value="buenos-aires">Buenos Aires</option>
      <option value="cancun">Cancún</option>
      <option value="madrid">Madrid</option>
      <!-- Agrega más ciudades si es necesario -->
    </select>
  </div>
  <div class="flex-1 w-full flex flex-col justify-end">
    <button class="w-full bg-pink-600 hover:bg-pink-700 text-white font-semibold py-3 px-6 rounded-2xl shadow-md transition flex items-center justify-center gap-2 text-base">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5">
        <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-4.35-4.35m0 0A7.5 7.5 0 104.5 4.5a7.5 7.5 0 0012.15 12.15z" />
      </svg>
      Buscar
    </button>
  </div>
</div>
